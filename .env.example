# Final deployment target: PostgreSQL
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/realtime_pipeline

# Local quick-start fallback (works without PostgreSQL installed)
DATABASE_URL=sqlite:///./pipeline.db
SIMULATION_INTERVAL_SECONDS=2
